<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta UpTask
            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") Email:
                    input(type="email" name="email" placeholder="Introduce tu correo electrónico" required)
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" placeholder="Elige tu contraseña" required)
                .campo.enviar
                    input.boton(type="submit" value="Crear mi cuenta")
                .campo
                    a(href="/iniciar-sesion") Iniciar sesión
        script(src="/dist/bundle.js")  